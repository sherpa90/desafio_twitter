
<div class="container">
<div class="container text-center">
<p style="color: green"><%= notice %></p>

<h1>Tweets</h1>

<%= form_with url: tweets_path, method: :get do |form| %>
<%= form_with(url: tweets_path, method: :get, class: "form-inline") do |form| %>
  <div class="form-group mb-2">
    <%= form.text_field :query_text, class: "form-control", placeholder: "Busque nomás profe", value: params[:query_text] %>
  </div>
  <%= form.submit "Buscar", class: "btn btn-primary mb-2" %>
<% end %>

<%end%>
</br>

<div id="tweets">
  <% @tweets.each do |tweet| %>
    <%= render tweet %>
    <p>
      <%= link_to "Mostrar este Tweet", tweet %>
    </p>
  </br>
  <% end %>
</div>
</br>
<%= link_to "Nuevo Tweet", new_tweet_path %>
</br>

<%== pagy_bootstrap_nav(@pagy) %>
</div>
</div>